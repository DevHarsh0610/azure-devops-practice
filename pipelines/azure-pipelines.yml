# pipelines/azure-pipelines.yml
# ─────────────────────────────────────────────────────────────────────────────
# Main Pipeline — Orchestrator
#
# This file is intentionally kept thin. It defines:
#   1. Triggers and PR conditions
#   2. Shared variables
#   3. Stages (with their conditions, dependencies, and environment targets)
#
# The actual job logic lives in templates/jobs/*.yml
# This separation means:
#   - Stages stay here because this is where approvals and conditions live
#   - Jobs are reusable templates that can be called with different parameters
# ─────────────────────────────────────────────────────────────────────────────

trigger:
  branches:
    include:
      - develop
      - release/*
      - main

pr:
  branches:
    include:
      - develop
      - release/*
      - main

pool:
  vmImage: ubuntu-latest

variables:
  imageRepository: 'dockerharsh10/devops-practice-backend'
  containerRegistry: 'dockerhub-service-principal'
  tag: '$(Build.BuildId)'
  azureSubscription: 'azure-arm-connection'

stages:
  # ── Stage 1: CI Validation ─────────────────────────────────────────────────
  # Runs on every push and PR. No branch restriction.
  - stage: CI_Validation
    displayName: 'CI & Code Quality Gate'

    jobs:
      - template: templates/jobs/validate-and-test.yml
        parameters:
          nodeVersion: '20.x'

  # ── Stage 2: Build & Push Docker Image ────────────────────────────────────
  # Runs on develop, release/*, and main — skipped on feature/bugfix branches.
  - stage: Build_Artifact
    displayName: 'Build and Push Docker Image'
    dependsOn: CI_Validation
    condition: and(succeeded(), or(eq(variables['Build.SourceBranch'], 'refs/heads/develop'), startsWith(variables['Build.SourceBranch'], 'refs/heads/release/'), eq(variables['Build.SourceBranch'], 'refs/heads/main')))

    jobs:
      - template: templates/jobs/build-and-push.yml
        parameters:
          imageRepository: $(imageRepository)
          containerRegistry: $(containerRegistry)
          tag: $(tag)

  # ── Stage 3: Deploy to Development ────────────────────────────────────────
  # Deploys after image is built. Targets the 'development' Azure DevOps
  # Environment — you can add approval gates there in the Azure DevOps portal.
  - stage: Deploy_Develop
    displayName: 'Deploy to Development Environment'
    dependsOn: Build_Artifact
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/develop'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'development'
          appName: 'my-unique-devops-app-123'
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'development'

  # ── Stage: Deploy to QA ───────────────────────────────────────────────────
  # Runs on release/* branches after Build passes.
  - stage: Deploy_QA
    displayName: 'Deploy to QA Environment'
    dependsOn: Build_Artifact
    condition: and(succeeded(), startsWith(variables['Build.SourceBranch'], 'refs/heads/release/'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'qa'
          appName: 'my-unique-devops-app-123'
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'qa'

  # ── Stage: Deploy to UAT ──────────────────────────────────────────────────
  # Runs after QA on release/* branches. Business stakeholders test here.
  # If UAT fails, the release branch is fixed — main is never touched.
  # Add a 'Pre-check approval' on the 'uat' ADO Environment in the portal.
  - stage: Deploy_UAT
    displayName: 'Deploy to UAT Environment'
    dependsOn: Deploy_QA
    condition: and(succeeded(), startsWith(variables['Build.SourceBranch'], 'refs/heads/release/'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'uat'
          appName: 'my-unique-devops-app-123'           # TODO: replace with real UAT App Service once provisioned
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'uat'

  # ── Stage: Deploy to Staging ──────────────────────────────────────────────
  # Runs on main after Build passes. First stop after code merges from release.
  - stage: Deploy_Staging
    displayName: 'Deploy to Staging Environment'
    dependsOn: Build_Artifact
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'staging'
          appName: 'my-unique-devops-app-123'
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'staging'

  # ── Stage 4: Deploy to Production ─────────────────────────────────────────
  # Runs on main after staging smoke tests pass.
  # Add a 'Pre-check approval' on the 'production' ADO Environment in the portal.
  - stage: Deploy_Prod
    displayName: 'Deploy to Production Environment'
    dependsOn: Deploy_Staging
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'production'
          appName: 'my-unique-devops-app-123'           # TODO: replace with real prod App Service once provisioned
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'production'

  # ── Stage 5: Deploy to DR ──────────────────────────────────────────────────
  # Runs after Production. Pre-check approval on 'dr' ADO Environment acts as
  # the 7-day wait gate before DR is promoted.
  - stage: Deploy_DR
    displayName: 'Deploy to DR Environment'
    dependsOn: Deploy_Prod
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))

    jobs:
      - template: templates/jobs/deploy-webapp.yml
        parameters:
          environment: 'dr'
          appName: 'my-unique-devops-app-123'           # TODO: replace with real DR App Service once provisioned
          azureSubscription: $(azureSubscription)
          imageRepository: $(imageRepository)
          tag: $(tag)

      - template: templates/jobs/smoke-test.yml
        parameters:
          appName: 'my-unique-devops-app-123'
          environment: 'dr'